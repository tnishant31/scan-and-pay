spring.application.name=payandscan
server.address=0.0.0.0
server.port=${PORT:8080}

# Dynamic URL for QR code scan
local.qr.url=${BACKEND_PUBLIC_URL:http://192.168.29.104:8080}/api/payments/scan-success/

# MongoDB Config (Supports cloud MongoDB Atlas via ENV or local fallback)
spring.mongodb.uri=${SPRING_MONGODB_URI:mongodb://localhost:27017/payandscan}

# Redis Config (Supports cloud Upstash Redis via ENV or local fallback)
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.ssl.enabled=${REDIS_SSL:false}

# Email Config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:flaskwebapplication@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ckqpoydvjcamkzhs}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true